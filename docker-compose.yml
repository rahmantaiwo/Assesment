services:

  backend:
    build: ./product-listing-backend

    ports:
      - "5000:5000"

    env_file:
      - ./product-listing-backend/.env

  frontend:
    build: ./frontend

    ports:
      - "5173:5173"

    depends_on:
      - backend